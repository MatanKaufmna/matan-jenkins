pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo building 1...'
                sh 'matan-test'
                sh ' test2'
                sh ' test 3'
            }
        }
    }
}
