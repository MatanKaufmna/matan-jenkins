pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo building ...'
                sh 'matan-test1'
                sh ' test2'
                sh ' test 3'

            }
        }
    }
}
