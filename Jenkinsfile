pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo building...'
                sh 'matan-test'
                sh ' test2'
            }
        }
    }
}
